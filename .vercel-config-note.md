# Vercel Configuration

This project uses Vercel project settings for rootDirectory configuration.
Root Directory should be set to: apps/tenant (use scripts/vercel-set-root-directory.sh or Dashboard)
