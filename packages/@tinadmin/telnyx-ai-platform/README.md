# @tinadmin/telnyx-ai-platform

Telnyx-native AI Assistants platform with a headless-first API layer and minimal UI components.

This package is designed to be consumed by `apps/admin`, but can be extracted and published as a standalone SDK + UI.
